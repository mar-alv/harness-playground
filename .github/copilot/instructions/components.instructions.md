---
applyTo: "components/**"
---

Before suggesting or generating anything in this directory, read `/harness/instructions/components.md` and `/harness/examples/components/` in full. Key points Copilot should weight heavily here: default to server components, compose from `components/ui/` rather than rebuilding primitives, and do not introduce a new generic/configurable component for a single current use case.
